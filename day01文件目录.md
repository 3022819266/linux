# Linux 根目录结构详解

Linux 文件系统遵循 **FHS（Filesystem Hierarchy Standard，文件系统层次结构标准）**，采用单根树状结构，所有文件和目录均从根目录 `/` 开始。

## 一、系统启动与核心命令

| 目录 | 全称 | 功能说明 | 典型内容 |
| :--- | :--- | :--- | :--- |
| `/` | Root | 文件系统起点，所有路径的根节点，普通用户仅有读取权限 | 所有子目录 |
| `/bin` | Binary | 存放**所有用户可用**的基础命令，系统启动和单用户模式下必需 | `ls`、`cp`、`mv`、`cat` |
| `/sbin` | System Binary | 存放**管理员专用**的系统管理命令，通常需 root 权限 | `fdisk`、`ifconfig`、`reboot` |
| `/boot` | Boot | 存放系统启动必需文件，**占满会导致无法开机** | 内核镜像（vmlinuz）、GRUB 引导程序、initramfs |
| `/lib` & `/lib64` | Library | 存放 `/bin` 和 `/sbin` 依赖的**共享库文件**（.so 文件）及内核模块 | `libc.so`、内核模块 |

> **注意**：现代 Linux 发行版（如 Ubuntu 16.04+、CentOS 7+）中，`/bin` 与 `/usr/bin`、`/sbin` 与 `/usr/sbin`、`/lib` 与 `/usr/lib` 通常为符号链接指向同一目录。

---

## 二、配置与用户目录

| 目录 | 功能说明 | 典型内容 |
| :--- | :--- | :--- |
| `/etc` | 系统**全局配置文件**目录，存放纯文本格式的系统和应用配置 | `/etc/passwd`（用户账户）、`/etc/hosts`、`/etc/fstab`（挂载配置）、`/etc/ssh/sshd_config` |
| `/home` | **普通用户**的主目录，每个用户拥有独立子目录，权限通常为 700 | `/home/username`（个人文件和配置） |
| `/root` | **超级用户（root）**的专属主目录，独立于 `/home`，普通用户无访问权限 | root 的个人配置文件和脚本 |

---

## 三、软件与资源目录

| 目录 | 全称 | 功能说明 | 典型内容 |
| :--- | :--- | :--- | :--- |
| `/usr` | Unix System Resources | 用户级程序与资源目录，类似 Windows 的 `Program Files` | `/usr/bin`（用户命令）、`/usr/lib`（库文件）、`/usr/share`（共享数据）、`/usr/local`（本地编译安装的软件） |
| `/opt` | Optional | 存放**第三方可选软件**，每个软件通常独占一个子目录 | `/opt/google/chrome/`、`/opt/mysql/` |

---

## 四、动态数据与临时文件

| 目录 | 全称 | 功能说明 | 典型内容 |
| :--- | :--- | :--- | :--- |
| `/var` | Variable | 存放**运行时可变数据**，文件大小会持续增长 | `/var/log`（系统日志）、`/var/cache`（缓存）、`/var/spool`（邮件/打印队列）、`/var/lib`（数据库文件） |
| `/tmp` | Temporary | **临时文件**目录，所有用户可读写，系统重启后通常清空 | 程序运行时的临时缓存、会话文件 |
| `/run` | Runtime | 存放系统**运行时数据**，挂载于内存，重启后清空 | PID 文件、socket、锁文件 |

---

## 五、设备与虚拟文件系统

| 目录 | 全称 | 功能说明 | 典型内容 |
| :--- | :--- | :--- | :--- |
| `/dev` | Device | **设备文件**目录，硬件设备被抽象为文件（一切皆文件） | `/dev/sda`（硬盘）、`/dev/null`（黑洞设备）、`/dev/tty`（终端） |
| `/proc` | Process | **虚拟文件系统**，数据存在于内存中，不占磁盘空间，反映内核和进程实时信息 | `/proc/cpuinfo`（CPU 信息）、`/proc/meminfo`（内存信息） |
| `/sys` | System | **虚拟文件系统**，提供内核与硬件设备的交互接口，用于设备管理和内核参数配置 | `/sys/class/`、`/sys/block/` |

> ️ `/proc` 和 `/sys` 是虚拟目录，**不要手动修改其中的文件**，否则可能导致系统异常。

---

## 六、挂载点目录

| 目录 | 功能说明 | 使用场景 |
| :--- | :--- | :--- |
| `/mnt` | **手动临时挂载点**，用于管理员手动挂载外部文件系统 | 挂载 NFS 共享、硬盘分区等 |
| `/media` | **自动挂载点**，系统自动识别并挂载可移动设备 | U盘、光盘、SD 卡等 |

---

## 七、其他特殊目录

| 目录 | 功能说明 |
| :--- | :--- |
| `/srv` | 存放**系统服务对外提供的数据**，如 Web 服务器网页文件（`/srv/www/`）、FTP 数据（`/srv/ftp/`） |
| `/lost+found` | 文件系统**修复时存放恢复的碎片文件**，仅 ext3/ext4 等文件系统存在，正常状态下为空 |
