
# Linux 进程监控、网络状态与包管理速查

> 涵盖实时进程监控 (`top`)、网络状态查看 (`netstat`) 以及 Debian/Ubuntu 系软件包管理 (`apt` & `dpkg`) 的核心命令与最佳实践。

## 目录

- [动态监控进程状态 (top)](#动态监控进程状态-top)
- [监控网络状态 (netstat)](#监控网络状态-netstat)
- [软件包管理 (apt 与 dpkg)](#软件包管理-apt-与-dpkg)
- [速记小结](#速记小结)
- [勘误与补充说明](#勘误与补充说明)

---

## 动态监控进程状态 (top)

`top` 命令类似于 `ps`，但它是一个**实时**监控工具，可以持续刷新并显示正在运行的进程状态。

### 常用启动选项

| 选项 | 说明 | 示例 |
| :--- | :--- | :--- |
| `-d` | 指定刷新间隔（秒），默认为 3 秒 | `top -d 1`（每秒刷新） |
| `-i` | 忽略闲置 (idle) 或僵死 (zombie) 进程 | `top -i` |
| `-p` | 仅监控指定的进程 ID (PID) | `top -p 1234` |
| `-u` | 仅显示指定用户的进程 | `top -u root` |
| `-c` | 显示完整的命令行路径，而非仅命令名 | `top -c` |

### 交互式快捷键

进入 `top` 界面后，可以直接按下以下按键进行操作：

| 按键 | 功能 | 说明 |
| :--- | :--- | :--- |
| `P` | **按 CPU 使用率排序** | 默认排序方式 |
| `M` | **按内存使用率排序** | 快速找出内存占用大户 |
| `N` | 按 PID 排序 | 按进程号大小排列 |
| `T` | 按累计 CPU 时间排序 | 找出运行时间最长的进程 |
| `k` | **终止进程** | 输入 PID 后回车，默认发送 SIGTERM (15) |
| `u` | 过滤用户 | 随后输入用户名，仅监控该用户进程 |
| `q` | 退出 | 退出 top 程序 |

> [!TIP]
> 在 `top` 交互界面按下 `k` 终止进程时，如果需要强制终止，可以在输入 PID 后，将信号值（Signal）修改为 **9** (SIGKILL)。

---

## 监控网络状态 (netstat)

`netstat` 用于显示网络连接、路由表、接口统计等信息，常用于排查网络故障。

### 常用选项

| 选项 | 说明 |
| :--- | :--- |
| `-a` | 显示所有活动的连接和监听端口 (all) |
| `-n` | 以数字形式显示地址和端口，不进行 DNS 解析 (numeric) |
| `-p` | 显示建立连接的进程 ID (PID) 和程序名称 (program) |
| `-t` | 仅显示 TCP 连接 (tcp) |
| `-u` | 仅显示 UDP 连接 (udp) |
| `-l` | 仅显示正在监听 (LISTEN) 的端口 |

### 常用组合命令

```bash
# 查看所有 TCP/UDP 监听端口及对应进程（最常用）
netstat -tulnp

# 查看所有连接及进程信息
netstat -anp

# 过滤特定端口（例如查看 80 端口）
netstat -anp | grep 80
```

> [!WARNING]
> 使用 `-p` 选项查看进程信息通常需要 **root 权限**（即使用 `sudo`），否则部分进程信息可能无法显示。

---

## 软件包管理 (apt 与 dpkg)

在 Debian/Ubuntu 系统中，`dpkg` 是底层工具，`apt` 是高级前端工具（能自动处理依赖）。

### 查看已安装的软件包

| 命令 | 说明 |
| :--- | :--- |
| `dpkg -l` | 列出所有已安装的软件包（推荐） |
| `apt list --installed` | 使用 apt 列出已安装的软件包 |

### 查询软件包详细信息

| 命令 | 说明 |
| :--- | :--- |
| `dpkg -s <软件名>` | 查看已安装软件的详细信息（状态、版本等） |
| `dpkg -L <软件名>` | 查看软件包安装后产生的所有文件列表 |
| `dpkg -S <文件路径>` | 反查某个系统文件属于哪个软件包 |
| `apt search <关键词>` | 在软件源中搜索软件包 |

### 安装与卸载

| 命令 | 说明 | 备注 |
| :--- | :--- | :--- |
| `apt install <软件名>` | 安装软件 | 自动解决依赖 |
| `apt remove <软件名>` | 卸载软件 | **保留**配置文件 |
| `apt purge <软件名>` | 彻底卸载 | **删除**配置文件及软件本体 |
| `apt autoremove` | 清理依赖 | 删除不再需要的孤立依赖包 |

> [!NOTE]
> **remove 与 purge 的区别：**
> - `apt remove`：仅删除二进制文件，保留 `/etc/` 下的配置文件，适合日后重装复用配置。
> - `apt purge`：删除二进制文件及系统级配置文件，适合彻底清理或配置损坏重置。

---

## 速记小结

| 场景 | 推荐命令 | 关键点 |
| :--- | :--- | :--- |
| 实时看谁占 CPU | `top` | 按 `P` 排序，按 `M` 看内存 |
| 查看端口占用 | `netstat -tulnp` | 需 `sudo` 才能看全进程 |
| 查文件属于哪个包 | `dpkg -S /path/to/file` | `dpkg` 强项 |
| 彻底卸载软件 | `apt purge <name>` | 连配置文件一起删 |
| 清理无用依赖 | `apt autoremove` | 释放磁盘空间 |

---
