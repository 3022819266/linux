
# Linux 定时任务调度速查手册（crontab 与 at）

> 覆盖周期性调度 `crontab` 与一次性调度 `at`：选项、时间字段、特殊符号、完整流程、备份恢复与任务管理。

## 目录

- [crontab 周期性定时任务](#crontab-周期性定时任务)
- [任务格式与时间字段](#任务格式与时间字段)
- [特殊符号说明](#特殊符号说明)
- [完整任务调度流程](#完整任务调度流程)
- [备份与恢复](#备份与恢复)
- [at 一次性定时任务](#at-一次性定时任务)
- [at 时间定义方式](#at-时间定义方式)
- [如何管理 at 任务](#如何管理-at-任务)
- [速记小结](#速记小结)
- [勘误说明](#勘误说明)

---

## crontab 周期性定时任务

`crontab` 用于配置**周期性**执行的定时任务调度。常用选项如下：

| 选项 | 说明 |
| :--- | :--- |
| `-e` | 编辑当前用户的 crontab 任务列表 |
| `-l` | 查看当前用户有哪些 crontab 任务 |
| `-r` | 删除当前用户**所有**的 crontab 任务 |
| `-i` | 删除前弹出确认提示，防止误删（建议与 `-r` 配合，即 `crontab -ri`）|

> 注意：`-r` 会清空所有任务。若只想删除部分任务，请进入编辑模式（`-e`），将要删除的那一行前加上 `#` 注释掉即可。

---

## 任务格式与时间字段

以一个例子说明：

```bash
* * * * * /bin/ls -l /etc/ > /home/lzy/1.txt
```

含义：**每分钟**执行一次 `ls -l /etc/`，并将其输出**覆盖写入** `/home/lzy/1.txt`。

其中前五个 `*` 是五个时间字段，顺序固定：

| 位置 | 字段 | 取值范围 |
| :--- | :--- | :--- |
| 第 1 个 | 一小时中的第几分钟 | `0-59` |
| 第 2 个 | 一天中的第几个小时 | `0-23` |
| 第 3 个 | 一个月中的第几天 | `1-31` |
| 第 4 个 | 一年中的第几个月 | `1-12` |
| 第 5 个 | 一周中的星期几 | `0-7`（`0` 与 `7` 都表示周日）|

> 记忆口诀：`分 时 日 月 周 命令`。

---

## 特殊符号说明

| 符号 | 含义 | 示例 |
| :--- | :--- | :--- |
| `*` | 任意时间（通配符）| 第 1 位的 `*` 表示每分钟都执行 |
| `,` | 同一字段内的多个离散时间点 | `0 8,12,16 * * *` → 每天 8:00、12:00、16:00 各执行一次 |
| `-` | 连续的时间范围 | `0 5 * * 1-6` → 周一至周六每天凌晨 5 点执行一次 |
| `*/n` | 每隔 n 个单位执行一次 | `*/10 * * * *` → 每 10 分钟执行一次 |

**举例：** 每隔一分钟把当前日期信息**追加**到 `1.txt`

```bash
*/1 * * * * /bin/date >> /home/lzy/1.txt
```

---

## 完整任务调度流程

1. 使用 `vim` 编辑 shell 脚本，注意脚本**首行必须加解释器声明**（如 `#!/bin/bash`）。
2. 使用 `chmod +x 脚本名.sh` 赋予脚本可执行权限。
3. 使用 `crontab -e` 进入任务调度编辑界面。
4. 键入 crontab 任务，注意**命令和文件务必使用绝对路径**。例如：

```bash
* * * * * /home/lzy/date.sh
```

---

## 备份与恢复

在执行 `crontab -r` 删除前，建议先备份以防后悔：

```bash
# 备份当前 crontab 任务
crontab -l > crontab_backup.txt

# 恢复：方式一（重定向）
crontab < crontab_backup.txt

# 恢复：方式二（直接指定文件）
crontab crontab_backup.txt
```

---

## at 一次性定时任务

`at` 用于执行**一次性**的定时任务，执行完毕后不再重复。

执行前，`atd` 守护进程会在后台检查作业队列，默认每 **60 秒**检查一次；若有作业且运行时间与当前时间匹配，则运行该作业。

> 使用 `at` 前，务必先确认 `atd` 进程已启动：

```bash
ps -ef | grep atd
```

**命令格式：**

```bash
at [-m] 时间
```

按下回车后进入 `at>` 提示符，输入需要执行的命令，最后按 **`Ctrl + D`** 结束编辑并保存任务。（输入错误可用 `Ctrl + 退格键` 删除。）

---

## at 时间定义方式

| 方式 | 示例 |
| :--- | :--- |
| 指定时分 | `04:00` |
| 模糊时间词 | `midnight`(午夜) / `noon`(中午) / `teatime`(下午4点) |
| 12 小时制 | `12pm` |
| 具体年月日时间 | `04:00 2021-03-01` |
| 相对计时 | `now + 5 minutes`（5 分钟后执行）|
| 日期关键字 | `today` / `tomorrow` |

**举例：** 在 2 天后的下午 5 点执行查看文件命令

```bash
at 5pm + 2 days
at> ls /home/lzy/1.txt
# 按一次 Ctrl + D 退出并提交任务
```

---

## 如何管理 at 任务

| 命令 | 说明 |
| :--- | :--- |
| `atq` | 查看当前有哪些 at 任务（显示任务编号）|
| `atrm 编号` | 删除指定的 at 任务 |
| `atrm`（不带编号）| 删除所有 at 任务 |

---

## 速记小结

| 场景 | 命令 | 关键提示 |
| :--- | :--- | :--- |
| 周期性任务 | `crontab -e` | 字段顺序：分 时 日 月 周；用绝对路径 |
| 查看周期任务 | `crontab -l` | 可重定向备份 |
| 删除全部周期任务 | `crontab -ri` | 加 `-i` 防误删 |
| 一次性任务 | `at 时间` | 先确认 `atd` 已启动 |
| 查看 at 队列 | `atq` | 显示任务编号 |
| 删除 at 任务 | `atrm 编号` | 不带编号则清空 |

---
