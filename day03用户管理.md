# Linux 用户与系统管理速查手册

## 👤 用户管理

| 命令 | 说明 | 备注 |
| :--- | :--- | :--- |
| `useradd 用户名` | 添加新用户 | - |
| `passwd 用户名` | 更改指定用户的密码 | - |
| `userdel 用户名` | 删除指定用户 | 仅删除用户账号 |
| `userdel -r 用户名` | 删除用户及其家目录 | ⚠️ 会彻底删除用户相关文件，一般不建议轻易使用 |

---

## 👥 用户组管理

| 命令 | 说明 |
| :--- | :--- |
| `groupadd 组名` | 新增一个用户组 |
| `groupdel 组名` | 删除指定的用户组 |
| `useradd -g 组名 用户名` | 创建新用户，并直接将其加入到指定的用户组中 |

---

## ⚙️ 指定运行级别

在 Linux 系统中，可以通过 `init` 命令来指定系统的运行级别（Runlevel）。各运行级别的含义如下：

| 级别 | 含义 |
| :---: | :--- |
| **0** | 关机 |
| **1** | 单用户模式 |
| **2** | 多用户模式（无网络服务） |
| **3** | 多用户模式（有网络服务） |
| **4** | 系统未使用，保留给用户自定义 |
| **5** | 图形界面模式 |
| **6** | 系统重启 |

**使用示例：**

```bash
init 3  # 切换到多用户有网络服务模式
```

---

## 🔑 找回 Root 密码（Ubuntu）

如果在 Ubuntu 系统中忘记了 root 密码，可以通过以下步骤进行重置：

1. **进入编辑模式**：在系统启动时，迅速按下键盘上的 **`e`** 键，进入 GRUB 编辑界面。
2. **进入恢复模式**：在菜单中选择并进入 **恢复模式（Recovery Mode）**。
3. **获取 Root Shell**：在恢复菜单中找到并选择进入 **root** 选项，以获取 root 权限的 Shell。
4. **重新挂载文件系统**：默认情况下文件系统是只读的，需要输入以下命令将其重新挂载为可读写：

   ```bash
   mount -o remount,rw /
   ```

5. **重置密码**：输入以下命令并按提示输入新密码：

   ```bash
   passwd root
   ```

6. **重启系统**：密码修改成功后，输入 `reboot` 命令重启系统即可使用新密码登录。

---

> 📝 **Note**: 本文档基于 Linux（Ubuntu）环境整理，命令执行需具备 root 或 sudo 权限。

