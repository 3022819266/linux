
# Linux 磁盘分区与挂载管理速查手册

> 覆盖硬盘设备命名规则、分区创建、格式化、临时/永久挂载、卸载，以及磁盘使用量查询与文件统计等实用指令。

## 目录

- [硬盘设备命名](#硬盘设备命名)
- [查看挂载情况](#查看挂载情况)
- [分区与挂载完整流程](#分区与挂载完整流程)
- [永久挂载（/etc/fstab）](#永久挂载etcfstab)
- [卸载分区](#卸载分区)
- [磁盘使用情况查询](#磁盘使用情况查询)
- [磁盘实用指令](#磁盘实用指令)
- [速记小结](#速记小结)
- [勘误说明](#勘误说明)

---

## 硬盘设备命名

硬盘按接口主要分为 **IDE** 与 **SCSI（含 SATA / USB）** 两类，当前绝大多数设备为 SCSI 类型。Linux 通过「设备类型 + 盘符 + 分区号」的形式标识磁盘分区。

| 接口 | 命名格式 | 盘符含义 | 示例 |
| :--- | :--- | :--- | :--- |
| **IDE** | `hd<x><n>` | `a`=第一块主盘，`b`=第一块从盘，`c`=第二块主盘，`d`=第二块从盘 | `hda1` → IDE 第一块主盘上的分区 1 |
| **SCSI / SATA / USB** | `sd<x><n>` | `a`、`b`、`c`、`d`… 按设备识别顺序依次排列 | `sdb1` → SCSI 第二块盘上的分区 1 |

> 命名规则：`sd` / `hd` 表示设备类型，`x`（a/b/c…）表示盘符，末尾数字 `n` 表示分区号。

---

## 查看挂载情况

| 命令 | 说明 |
| :--- | :--- |
| `lsblk` | 以树状列出块设备及其挂载情况 |
| `lsblk -f` | 显示更详细信息：文件系统类型、UUID、挂载点等 |

```bash
# 查看设备与分区概览
lsblk

# 查看文件系统类型、UUID、挂载点等详细信息
lsblk -f
```

---

## 分区与挂载完整流程

### 1. 添加硬盘到设备

将新硬盘接入主机，确认可被识别（可先用 `lsblk` 查看）。

### 2. 创建磁盘分区

```bash
# 进入 fdisk 交互界面，对目标硬盘分区
fdisk /dev/硬盘名
```

在 `fdisk` 交互提示符下常用操作：

| 按键 | 含义 |
| :--- | :--- |
| `n` | 新建一个分区 |
| `p` | 主分区（primary） |
| `e` | 扩展分区（extended） |
| 数字 | 输入分区号，数字为几即表示分几个区 |
| `w` | 保存并退出 |
| `q` | 不保存退出 |

### 3. 格式化磁盘

```bash
# 按指定文件系统类型格式化目标分区
mkfs -t 文件系统类型 /dev/硬盘名分区

# 例：将 sdb1 格式化为 ext4
mkfs -t ext4 /dev/sdb1
```

### 4. 挂载分区

```bash
# 先创建挂载目录
mkdir /mnt/data

# 临时挂载
mount /dev/硬盘名分区 挂载目录
```

> [!WARNING]
> `mount` 属于**临时挂载**，系统重启后会失效。需永久挂载请见下一节。

---

## 永久挂载（/etc/fstab）

编辑 `/etc/fstab` 文件，让分区在开机时自动挂载：

```bash
vim /etc/fstab
```

按列填写一行配置，各字段含义如下：

| 字段 | 内容 | 示例 |
| :--- | :--- | :--- |
| 第 1 列 | UUID 或设备路径 | `UUID=xxxx` 或 `/dev/sdb1` |
| 第 2 列 | 挂载目录 | `/mnt/data` |
| 第 3 列 | 文件系统类型 | `ext4` |
| 第 4 列 | 挂载选项 | `defaults` |
| 第 5 列 | 是否被 dump 备份 | `0` |
| 第 6 列 | 开机 fsck 检查顺序 | `0` |

```bash
# /etc/fstab 中添加（推荐用 UUID，避免设备名漂移）
UUID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx  /mnt/data  ext4  defaults  0 0
```

> 修改 `/etc/fstab` 后可执行 `mount -a` 立即测试配置是否正确，避免重启后挂载失败。

---

## 卸载分区

```bash
# 通过设备名卸载
umount /dev/硬盘名分区

# 或通过挂载目录卸载
umount 挂载目录
```

> [!TIP]
> 卸载前确保没有进程正在使用该目录（可用 `lsof 挂载目录` 或 `fuser -m 挂载目录` 查看），否则 `umount` 会提示设备忙。

---

## 磁盘使用情况查询

| 命令 | 说明 |
| :--- | :--- |
| `df -h` | 查看系统整体磁盘使用情况（`-h` 以可读单位显示） |
| `du 选项 目录` | 查看指定目录占用情况 |

`du` 常用选项：

| 选项 | 含义 |
| :--- | :--- |
| `-s` | 仅汇总指定目录的占用大小 |
| `-h` | 带可读计量单位（K/M/G） |
| `-a` | 统计时包含文件 |
| `--max-depth=N` | 限制子目录显示深度 |
| `-c` | 列出明细的同时在末尾追加总计 |

```bash
# 查看系统整体使用情况
df -h

# 查看 /home 目录的使用情况（含文件、带单位、明细+总计、深度1）
du -ahc --max-depth=1 /home
```

---

## 磁盘实用指令

| 指令 | 作用 |
| :--- | :--- |
| `ls -l 目录 \| grep "^-" \| wc -l` | 统计目录下**文件**个数（不含子目录） |
| `ls -l 目录 \| grep "^d" \| wc -l` | 统计目录下**子目录**个数（把 `-` 改为 `d`） |
| `ls -lR 目录 \| grep "^-" \| wc -l` | 递归统计目录下文件个数（含子目录） |
| `ls -lR 目录 \| grep "^d" \| wc -l` | 递归统计目录个数（含子目录） |
| `tree 目录` | 以树状结构显示目录层级 |

```bash
# 统计 /home 下的文件个数
ls -l /home | grep "^-" | wc -l

# 统计 /home 下包含子目录的文件个数
ls -lR /home | grep "^-" | wc -l

# 树状显示目录结构
tree /home
```

> `ls -l` 输出的每行首字符标识类型：`-` 表示普通文件，`d` 表示目录。`grep "^-"` / `grep "^d"` 正是据此过滤计数。

---

## 速记小结

| 场景 | 命令 | 关键提示 |
| :--- | :--- | :--- |
| 查看设备/挂载 | `lsblk -f` | 含文件系统类型、UUID、挂载点 |
| 创建分区 | `fdisk /dev/sdX` | `n` 新建 → `p`/`e` 选类型 → `w` 保存 |
| 格式化 | `mkfs -t ext4 /dev/sdXn` | `-t` 后是文件系统类型 |
| 临时挂载 | `mount /dev/sdXn 目录` | 重启失效 |
| 永久挂载 | 编辑 `/etc/fstab` | 推荐 UUID；改后 `mount -a` 测试 |
| 卸载 | `umount 设备/目录` | 先确认无进程占用 |
| 整体用量 | `df -h` | - |
| 目录用量 | `du -ahc --max-depth=1 目录` | - |
| 统计文件数 | `ls -lR 目录 \| grep "^-" \| wc -l` | 目录改 `^d` |

---

