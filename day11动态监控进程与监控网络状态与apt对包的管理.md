动态监控进程状态
top 它与ps相似但top在执行的一段时间内可以更新正在运行的进程
top 选项
-d 秒数 
指定top命令每隔几秒更新，默认三秒
-i 使top不显示任何闲置或者僵死的进程
-p 通过指定监控进程的id来仅仅监控某个进程状态

当输入top命令后直接回车可以进入操作交互此时输入
P 按照cpu使用率排序，默认的排序方式就是按照cpu使用率排序
M 以内存的使用率排序
N 以pid排序
q 退出top
u 随后输入用户名可以监控指定的用户
K 然后输入PID以终止进程，带上-9可以强制终止进程

监控网络状态
netstat 选项
-an 按一定顺序排列输出
-p 显示那个进程在调用

apt对于包的管理
dpkg 与 apt都可以查看已安装的软件包
使用 dpkg -qa 或者 apt list --installed
dpkg -s 软件名 查看已安装软件的详细信息
dpkg -S 系统文件/目录 查看某个系统软件属于那个软件包
dpkg -L 软件名 查看软件包中包含的所有文件
卸载软件包
apt remove 软件名 此命令通常会保留用户的配置文件
apt purge 软件名 此命令会强制删除软件并且不保留配置文件
apt install 软件名 此命令用于安装软件
apt search 软件名 可以用来查看是否安装了这样一个文件




