
# Linux 文件与目录管理命令速查手册



## 目录

- [查看目录与文件](#查看目录与文件)
- [目录的创建与删除](#目录的创建与删除)
- [文件的创建与删除](#文件的创建与删除)
- [复制与移动](#复制与移动)
- [查看文件内容](#查看文件内容)
- [输出与重定向](#输出与重定向)
- [链接](#链接)
- [历史命令](#历史命令)

---

## 查看目录与文件

| 命令 | 说明 |
| :--- | :--- |
| `ls` | 查看当前目录内容 |
| `ls -a` | 查看目录下所有文件（含隐藏文件） |
| `ls -l` | 以列表形式显示文件详细信息（可设别名 `ll`） |
| `cd 目录` | 切换到指定目录 |
| `cd ..` | 切换到上一级目录 |
| `cd ~` | 回到自己的家目录 |

```bash
ls -a        # 列出所有文件（包括以 . 开头的隐藏文件）
ls -l        # 长列表显示权限、大小、修改时间
cd /etc      # 进入 /etc 目录
cd ..        # 返回上一级目录
cd ~         # 回到 home 目录
```

---

## 目录的创建与删除

| 命令 | 说明 | 备注 |
| :--- | :--- | :--- |
| `mkdir 目录名` | 创建单层目录 | - |
| `mkdir -p a/b/c` | 创建多级目录 | 一次性补全所有缺失的父级目录 |
| `rmdir 目录名` | 删除空目录 | 目录非空时无法删除 |

```bash
mkdir test            # 创建 test 目录
mkdir -p a/b/c        # 递归创建 a/b/c 多级目录
rmdir test            # 删除空的 test 目录
```

---

## 文件的创建与删除

| 命令 | 说明 | 备注 |
| :--- | :--- | :--- |
| `touch 文件名` | 创建空文件，或更新文件时间戳 | 文件已存在时只更新修改时间 |
| `rm 文件名` | 删除文件 | - |
| `rm -r 目录名` | 递归删除非空目录 | 逐层删除目录及其内容 |
| `rm -rf 目录名` | 强制删除文件及目录且不提示 | 高危，不可恢复，慎用 |

```bash
touch note.txt        # 创建空文件
rm note.txt           # 删除文件
rm -r mydir           # 递归删除目录
rm -rf mydir          # 强制删除，不弹出确认
```

> 注意：`rm -rf` 没有回收站，执行前务必确认路径，避免误删系统文件。

---

## 复制与移动

| 命令 | 说明 |
| :--- | :--- |
| `cp 源 目标` | 复制文件到目标位置 |
| `cp -r 源目录 目标位置` | 递归复制目录及其中的所有内容；遇到同名文件会提示是否覆盖 |
| `\cp 源 目标` | 绕过别名强制覆盖且不提示 |
| `mv 文件1 文件2` | 同一目录下为「重命名」；跨目录时为「移动文件1 到目标位置」 |

```bash
cp a.txt /tmp/            # 复制 a.txt 到 /tmp
cp -r project /backup     # 递归复制整个 project 目录
\cp a.txt b.txt          # 强制覆盖，不弹确认
mv old.txt new.txt        # 重命名
mv a.txt /data/           # 将 a.txt 移动到 /data 目录
```

> 说明：很多发行版默认把 `cp` 别名设为 `cp -i`（交互确认），所以加反斜杠 `\cp` 可临时取消别名，实现强制覆盖、不提示。

---

## 查看文件内容

| 命令 | 说明 |
| :--- | :--- |
| `cat 文件` | 一次性查看文件全部内容 |
| `cat -n 文件` | 查看文件内容并显示行号 |
| `less 文件` | 分屏翻页查看（适合大文件，按 `q` 退出） |
| `head 文件` | 查看文件开头内容，默认前 10 行 |
| `head -n 数字 文件` | 查看文件前 N 行内容 |
| `tail 文件` | 查看文件末尾内容，默认后 10 行 |
| `tail -n 数字 文件` | 查看文件末尾 N 行内容 |
| `tail -f 文件` | 实时监控文件是否有新内容更新 |

```bash
cat -n app.log        # 带行号查看全部
less bigfile.txt      # 翻页查看，按 q 退出
head -n 20 conf.yml   # 只看前 20 行
tail -n 50 err.log    # 只看末尾 50 行
tail -f server.log    # 实时追踪日志输出
```

---

## 输出与重定向

| 符号 / 命令 | 说明 |
| :--- | :--- |
| `echo 内容` | 将内容输出到终端 |
| `>` | 重定向输出，把写入内容「覆盖」到目标文件 |
| `>>` | 重定向输出，把写入内容「追加」到目标文件末尾 |

```bash
echo "hello"                 # 终端打印 hello
echo "hello" > note.txt      # 覆盖写入 note.txt
echo "world" >> note.txt     # 追加一行到 note.txt
ls -l > list.txt             # 把 ls 的结果写入文件
```

---

## 链接

| 命令 | 说明 |
| :--- | :--- |
| `ln` | 创建链接（默认为硬链接），作用类似 Windows 快捷方式，用于存放指向其他文件 / 目录的路径 |
| `ln -s 源文件或目录 软链接名` | 给源文件或目录创建一个软（符号）链接 |

```bash
ln -s /var/www/html web      # 创建指向网站目录的软链接
ls -l web                    # 显示为 web -> /var/www/html
```

---

## 历史命令

| 命令 | 说明 |
| :--- | :--- |
| `history` | 查看使用过的历史指令列表（带编号） |
| `!数字` | 重新执行历史列表中编号为「数字」的那条指令 |

```bash
history        # 列出历史命令及编号
!42            # 重新执行编号为 42 的命令
```

---

## 速记小结

- **查看**：`ls` / `cd` / `cat` / `less` / `head` / `tail`
- **创建**：`mkdir` / `touch`
- **删除**：`rmdir`（空目录）/ `rm` / `rm -rf`（高危）
- **拷贝与移动**：`cp` / `mv`
- **重定向**：`>`（覆盖）/ `>>`（追加）
- **链接**：`ln -s`
- **历史**：`history` / `!n`
