# 远程登陆：
## 使用 **MobaXterm_Personal_26.4.exe** 实现 Windows 远程登录 Linux 服务器。
### 1. 基本连接步骤
1. 打开软件，点击左上角 **Session** 按钮。
2. 选择 **SSH** 协议。
3. 填写 **Remote host**（Linux 服务器 IP 地址）。
4. 勾选 **Specify username** 并输入用户名（如 root）。
5. 端口默认 **22**，点击 **OK** 后输入密码即可连接。

# Vim 编辑器基础

Vim 是 Linux 下最常用的文本编辑器，核心特点是 **模式化操作**。

## 一、三种基本模式

| 模式 | 说明 | 进入方式 |
| :--- | :--- | :--- |
| **普通模式（Normal）** | 默认模式，用于光标移动、复制、删除等操作 | 启动 Vim 即进入，或按 `Esc` 从其他模式返回 |
| **插入模式（Insert）** | 用于输入和编辑文本内容 | 普通模式下按 `i`、`a`、`o` 等键进入 |
| **命令行模式（Command）** | 用于保存、退出、查找替换等全局操作 | 普通模式下按 `:` 进入 |

### 二、模式切换

1.普通模式 → 插入模式：按 i（光标前插入）、a（光标后插入）、o（下方新开一行） <br>
2.插入模式 → 普通模式：按 Esc 键 <br>
3.普通模式 → 命令行模式：按 : 键 <br>
4.命令行模式 → 普通模式：按 Esc 键 或 执行完命令后自动返回 <br>

## 三、常用快捷键

### 普通模式下

| 操作 | 快捷键 | 说明 |
| :--- | :--- | :--- |
| **复制** | `yy` | 复制当前行 |
| | `nyy` | 复制包括本行在内的 n 行（如 `3yy` 复制 3 行） |
| **删除** | `dd` | 删除（剪切）当前行 |
| | `ndd` | 删除包括本行在内的 n 行（如 `3dd` 删除 3 行） |
| **粘贴** | `p` | 在光标下方粘贴 |
| | `P` | 在光标上方粘贴 |
| **撤销** | `u` | 撤销上一步操作 |
| **定位** | `gg` | 跳转到文件首行 |
| | `G` | 跳转到文件末行 |
| | `nG` | 跳转到第 n 行（如 `10G` 跳到第 10 行） |

### 命令行模式下

| 操作 | 命令 | 说明 |
| :--- | :--- | :--- |
| **保存** | `:w` | 保存文件 |
| **退出** | `:q` | 退出（未修改时可用） |
| **保存并退出** | `:wq` | 保存文件并退出 |
| **强制退出** | `:q!` | 强制退出不保存 |
| **查找** | `/关键字` | 向下查找关键字 |
| | `n` | 查找下一个匹配项 |
| | `N` | 查找上一个匹配项 |

## 四、快速上手流程

```bash
# 1. 打开或创建文件
vim filename.txt

# 2. 按 i 进入插入模式，编辑文本

# 3. 按 Esc 返回普通模式

# 4. 输入 :wq 保存并退出
