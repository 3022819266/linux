
# Linux 持久化任务与会话管理、日志查看速查

> 涵盖防止终端挂断的 `nohup`、保留工作现场的 `screen` / `tmux` 会话复用工具，以及 systemd 集中式日志查询工具 `journalctl`。

## 目录

- [持久化工具概述](#持久化工具概述)
- [nohup 防挂断](#nohup-防挂断)
- [标准输出与错误重定向](#标准输出与错误重定向)
- [screen 会话保持](#screen-会话保持)
- [tmux 会话保持](#tmux-会话保持)
- [screen 与 tmux 对比](#screen-与-tmux-对比)
- [服务日志查看 journalctl](#服务日志查看-journalctl)
- [速记小结](#速记小结)


---

## 持久化工具概述

普通的后台任务在终端关闭（挂断）后会被终止。Linux 提供两类工具解决这个问题：

| 工具 | 作用 | 适用场景 |
| :--- | :--- | :--- |
| `nohup` | 防挂断，让程序在终端断开后继续运行 | 一次性、无需交互的快速后台任务（备份、文件传输） |
| `screen` / `tmux` | 保留完整工作现场，重连后可恢复 | 长时间运行、需要随时回来查看输出或交互的复杂操作 |

---

## nohup 防挂断

当终端断开后，`nohup` 可以让程序继续**在后台运行**，适用于一次性、无需交互的快速后台任务（如简单数据备份或文件传输）。

**命令格式：**

```bash
nohup 命令/服务/程序 > 日志文件 2>&1 &
```

> [!TIP]
> - `>`：将标准输出重定向写入日志文件。
> - `&`：放在行尾，表示放到**后台**运行。
> - 单独使用 `nohup` 而不指定重定向时，默认会把输出写到当前目录的 `nohup.out`。

**示例：** 执行 `1.py` 并将其正常输出与报错一并写入 `1.txt`，全程后台运行：

```bash
nohup python 1.py > 1.txt 2>&1 &
```

---

## 标准输出与错误重定向

Linux 的三个标准文件描述符（fd）是理解重定向的关键：

| 编号 | 名称 | 含义 |
| :--- | :--- | :--- |
| `0` | stdin | 标准输入 |
| `1` | stdout | 标准输出（正常结果） |
| `2` | stderr | 标准错误（报错信息） |

`2>&1` 的含义是：**把标准错误（`2`）重定向到标准输出（`1` 当前指向的位置）**，从而让报错和正常输出一起写入同一个文件。

> [!NOTE]
> 写作 `> 1.txt 2>&1` 时，先把 `1`（标准输出）指向 `1.txt`，再让 `2` 跟随 `1`，因此正常输出与报错**都进入 `1.txt`**。
> 如果写成 `2>&1 > 1.txt`（顺序颠倒），报错会指向终端而不会进入文件——顺序不能错。

---

## screen 会话保持

`screen` 在终端断开后保留当前工作状态，重连后可恢复到之前的现场。

| 操作 | 命令 / 快捷键 |
| :--- | :--- |
| 创建命名会话 | `screen -S 会话名` |
| 退出（脱离）当前会话 | `Ctrl + A` 松开后再按 `D`（detach） |
| 重新连接会话 | `screen -r 会话名` |
| 查看会话列表 | `screen -ls` |

---

## tmux 会话保持

`tmux` 与 `screen` 作用相同，同样可保留现场并在重连后恢复，且功能更强大，**通常比 `screen` 更推荐**。

| 操作 | 命令 / 快捷键 |
| :--- | :--- |
| 创建命名会话 | `tmux new -s 会话名` |
| 退出（脱离）当前会话 | `Ctrl + B` 松开后再按 `D`（detach） |
| 重新连接会话 | `tmux attach -t 会话名`（可简写 `tmux a -t 会话名`） |
| 查看会话列表 | `tmux ls` |

---

## screen 与 tmux 对比

| 维度 | screen | tmux |
| :--- | :--- | :--- |
| 创建会话 | `screen -S 名` | `tmux new -s 名` |
| 脱离前缀键 | `Ctrl + A` 后按 `D` | `Ctrl + B` 后按 `D` |
| 重连会话 | `screen -r 名` | `tmux attach -t 名` |
| 查看列表 | `screen -ls` | `tmux ls` |
| 推荐度 | 通用、轻量 | 面板分割、脚本化更强，**更推荐** |

---

## 服务日志查看 journalctl

`journalctl` 是 **systemd** 的集中式日志查询工具，用于检索由 `systemd-journald` 收集的**结构化日志**（无需翻找传统文本日志文件）。

| 命令 | 说明 |
| :--- | :--- |
| `journalctl -u 服务名` | 查看指定服务的所有日志 |
| `journalctl -u 服务名 -f` | 实时跟踪最新日志输出（类似 `tail -f`） |
| `journalctl -u 服务名 --since "2026-09-25 10:00:00"` | 查看某时间点之后的日志 |
| `journalctl -u 服务名 -p err` | 只看优先级为 error 及以上的日志 |
| `journalctl -xe` | 查看最近日志并定位到末尾，附带解释（排障常用） |

> [!TIP]
> `--since` / `--until` 支持相对写法，如 `--since "1 hour ago"`、`--since today`，便于快速过滤时间窗口。

---

## 速记小结

| 场景 | 推荐命令 | 关键点 |
| :--- | :--- | :--- |
| 一次性后台任务 | `nohup 命令 > log 2>&1 &` | 末尾 `&` 放后台，`2>&1` 合并报错 |
| 保留现场可恢复 | `tmux new -s 名` / `tmux a -t 名` | 前缀键 `Ctrl+B`，再按 `D` 脱离 |
| 轻量会话复用 | `screen -S 名` / `screen -r 名` | 前缀键 `Ctrl+A`，再按 `D` 脱离 |
| 看服务日志 | `journalctl -u 服务名` | 实时跟踪加 `-f` |
| 只看报错 | `journalctl -u 服务名 -p err` | 配合 `--since` 缩小范围 |

---
