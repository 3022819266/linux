
ls 查看
ls -a 查看目录下的所有文件
ls -l 以列表形式显示文件
cd 切换目录
cd .. 切换到上一级目录
cd ~/cd : 回到自己的家目录
mkdir 创建目录
mkdir -p 创建多级目录
rmdir 删除空目录
rm 删除文件
rm -r 删除非空目录
rm -rf 删除文件以及文件目录且不提示
touch 创建文件
cp 复制
cp 文件名 位置 将一个目录以及目录中的所有内容复制到目标位置
cp -r 文件名 位置 当有相同的文件时提示是否要覆盖
\cp 强制覆盖且不提示
mv 文件1 文件2 若文件1文件2位于同一目录下则为重命名
mv /文件 /文件 将文件1移动至文件二处
cat 查看文件内容
cat -n 查看文件内容并且显示行号
less 分屏查看文件内容（大文件）
echo 输出内容到终端
head 查看文件的开通内容默认10行
head -n 数字 文件名 查看文件的前几行内容
tail 查看文件尾部内容默认10行
tail -n 数字 文件名 产看文件的末尾几行内容
tail -f 文件名 监控文件看是否有内容更新
>  将写入的内容覆盖到
>> 将写入的内容追加到
ln 类似于windows的快捷方式用于存放链接其他文件的路径
ln -s 原文件夹或目录 软连接名 给原文件夹创建一个软链接
history 查看使用过的历史指令
! 数字 执行第几个指令
