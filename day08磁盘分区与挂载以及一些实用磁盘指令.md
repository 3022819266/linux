linux中的分区：
硬盘可以分为ide scsi两种，目前大多数为scsi
ide:
hdx~ hd表示设备类型，x表示为盘符如a(基本盘),b（基本从属盘）,c（辅助主）,d（辅助从属盘），~表示分区，如hda1表示ide硬盘a盘上的分区1
sdx~ sd表示设备类型，x表示为盘符如a,b,c,d,~表示分区，如sdb1表示为scsi硬盘b盘上的分区1
lsblk 与 lsblk -f 查看设备挂载情况
使用lsblk -f可以查看到更详细的信息，uiud,挂载点等

挂载分区至目录下：
1.添加硬盘至设备当中
2.创建磁盘分区
  fdisk /dev/硬盘名
  n 创建一个新的分区
  p 主要分区 e 扩展分区 根据情况选择p还是e
  输入分区号，输入数字几就是分几个区
  w 保存并退出
3.格式化磁盘
  mkfs -t 分区类型 /dev/硬盘名
4.进行挂载
  创建挂载目录
  mount /dev/硬盘名 挂载目录
注意:此方法在重启之后会失效因此选择下面这个方法可以永久挂载
  1.编辑/etc/fstab文件，输入vim /etc/fstab
  2.按列输入uiud(或者/dev/硬盘名)，挂载目录，挂载的分区类型，第四列可以保持defaults,第五列0 0
卸载
  umount /dev/硬盘名或者 umount 挂载目录

磁盘使用情况查询：
  df -h 查看系统整体使用情况
  du 选项 目录 查询指定目录使用情况
  -s 指定目录占用大小汇总
  -h 带计量单位
  -a 含文件
  --max-depth=1 子目录深度
  -c 列出明细的同时增加汇总值
  如查询/home目录的使用情况
  du -l -hac --max-depth=1 /hone

磁盘实用指令：
  ls -l 目录 | grep "^-" | wc -l 统计目录下的文件个数
  同理统计目录个数只需要将-改为d即可
  ls -lR 目录 | grep "^-" | wc -l 统计目录下的文件个数包括子目录下的文件
   同理统计目录个数包括目录下的子目录只需要将-改为d即可

   tree 目录 将目录以树状显示



  
  
