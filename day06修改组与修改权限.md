
# Linux 文件归属与权限管理速查手册

> 按功能整理，覆盖「修改所有者 / 所在组」与「权限位解读 + chmod 修改权限」两大主题，符合 GitHub Flavored Markdown 规范，可直接渲染。

## 目录

- [一、修改归属（所有者 / 所在组）](#一修改归属所有者--所在组)
- [二、权限位解读](#二权限位解读)
- [三、修改权限（chmod）](#三修改权限chmod)
- [四、速记小结](#四速记小结)
- [五、勘误与补充说明](#五勘误与补充说明)

---

## 一、修改归属（所有者 / 所在组）

| 命令 | 作用 | 示例 |
| :--- | :--- | :--- |
| `chown 用户名 文件` | 修改文件的**所有者** | `chown lzy 1.txt` |
| `chgrp 组名 文件` | 修改文件的**所在组** | `chgrp ceshi 1.txt` |
| `usermod -g 新组名 用户名` | 改变用户所在的**主组** | `usermod -g dev lzy` |

```bash
# 修改文件所有者
chown lzy 1.txt

# 修改文件所在组
chgrp ceshi 1.txt

# 同时修改所有者和所在组（user:group 写法）
chown lzy:ceshi 1.txt

# 改变用户的主组
usermod -g dev lzy
```

> 提示：`chown` / `chgrp` 修改目录时加 `-R` 可递归作用于其下所有文件与子目录；批量修改归属通常需要 `sudo`。

---

## 二、权限位解读

以 `ll` 的一行输出为例：

```text
-rwxr-x--x 1 lzy ceshi 203 9月 4 20:36 1.txt
```

各字段含义如下：

| 位置 | 内容 | 含义 |
| :--- | :--- | :--- |
| 第 1 位 | `-` | 文件类型 |
| 第 2–4 位 | `rwx` | **所有者(u)** 权限 |
| 第 5–7 位 | `r-x` | **所在组(g)** 权限 |
| 第 8–10 位 | `--x` | **其他人(o)** 权限 |
| 第 11 字段 | `1` | 硬链接数（文件为 1；目录为其下子项数量） |
| 第 12 字段 | `lzy` | 所有者（user） |
| 第 13 字段 | `ceshi` | 所在组（group） |
| 第 14 字段 | `203` | 文件大小（字节） |
| 第 15–17 字段 | `9月 4 20:36` | 最后修改时间 |
| 末字段 | `1.txt` | 文件名 |

### 第 1 位：文件类型

| 符号 | 类型 | 说明 |
| :--- | :--- | :--- |
| `-` | 普通文件 | 文本、二进制等 |
| `d` | 目录 | directory |
| `l` | 链接文件 | symbolic link，软链接 |
| `c` | 字符设备 | 如鼠标、键盘等 character device |
| `b` | 块设备 | block device，如磁盘 |

### r / w / x 在「文件」与「目录」上的不同含义

| 权限 | 作用于文件 | 作用于目录 |
| :--- | :--- | :--- |
| `r` 可读 | 查看文件内容 | 列出目录下的文件名 |
| `w` 可写 | 修改文件**内容** | 在目录中**创建 / 删除 / 重命名**子项 |
| `x` 可执行 | 执行脚本 / 程序 | **进入**该目录（可 `cd` 进去） |

> 关键区别：一个文件能否被**删除**，不取决于文件自身的 `w`，而取决于它**所在父目录**是否拥有 `w` 权限。

### 数字表示法

`rwx` 可用数字叠加表示：

| 字符 | 数值 |
| :--- | :--- |
| `r` | 4 |
| `w` | 2 |
| `x` | 1 |
| `-` | 0 |

例如 `-rwxr-x--x` 即 `7 5 1` → **751**。

---

## 三、修改权限（chmod）

`chmod` 改变目标权限，可用**符号模式**或**数字模式**。

### 符号模式

- 操作符：`+` 增加、`-` 去除、`=` 赋值
- 对象：`u` 所有者、`g` 所在组、`o` 其他人、`a` 所有人（= u+g+o）

```bash
# 给所有者增加可执行权限
chmod u+x 1.txt

# 去除其他人的可读权限，同时给所在组增加可执行权限
chmod o-r,g+x 1.txt
```

### 数字模式

`r=4 w=2 x=1`，按 u/g/o 三组各相加得到三位八进制：`rwx=7`、`rw-=6`、`r-x=5`、`r--=4`、`--x=1`。

| 需求 | 命令 | 拆解 |
| :--- | :--- | :--- |
| 所有者 rwx、组 rw、其他 r | `chmod 764 1.txt` | `7=rwx`、`6=rw-`、`4=r--` |
| 所有者去 x、组加 x、其他去 r | `chmod 674 1.txt` | `6=rw-`、`7=rwx`、`4=r--` |

> 例：`chmod 764` 表示所有者可读可写可执行、所在组可读可写、其他人只读；`chmod 674` 则相对 `764` 是「所有者减去 x、所在组加上 x」的效果。

---

## 四、chown 与 chgrp 进阶用法

| 用法 | 说明 |
| :--- | :--- |
| `chown 新用户 文件/目录` | 只改所有者 |
| `chown 新用户:新组 文件/目录` | 同时改所有者和所在组（注意拼写为 `newgroup`） |
| `chown -R 新用户:新组 目录` | 递归改目录及其下所有文件 |
| `chgrp 新组 文件/目录` | 用法与 `chown` 一致，改所在组 |

```bash
# 把 ceshi 目录及其下所有文件/目录改为 lzy 拥有、ceshi 组
chown -R lzy:ceshi ceshi
```

---

## 五、速记小结

| 场景 | 命令 | 要点 |
| :--- | :--- | :--- |
| 改所有者 | `chown 用户 文件` | `-R` 递归，常配 `sudo` |
| 改所在组 | `chgrp 组 文件` | 也可 `chown 用户:组 文件` 同改 |
| 改用户主组 | `usermod -g 组 用户` | 修改账号归属，非改文件 |
| 看权限 | `ll` / `ls -l` | 第 1 位类型，后 9 位分 u/g/o |
| 改权限 | `chmod 数字/符号 文件` | `r=4 w=2 x=1`；目录 `w` 管增删改名 |

---

## 六、勘误与补充说明

| 原文 | 修正 / 补充 | 原因 |
| :--- | :--- | :--- |
| `chown newowner:nergroup` | 拼写为 `newowner:newgroup` | 原文 `nergroup` 为笔误 |
| 「第 1–3 位表示所有者权限」 | 明确为第 2–4 位属 u、5–7 属 g、8–10 属 o | 第 1 位是文件类型，权限位从第 2 位起算 |
| 时间写作 `20.36` | 改为 `20:36`（冒号分隔时分） | `ls -l` 输出时分用冒号 |
| `chmod 674` 未配对应操作说明 | 补「所有者去 x、组加 x、其他去 r」的语义对照 | 便于把文字需求翻译成三位数字 |
| `chown` 仅给「用户名 文件」 | 补充 `user:group` 同改与 `-R` 递归用法 | 实际常用写法 |
