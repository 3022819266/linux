crontab
crontab 周期性定时任务调度
crontab 常用选项
-e：编辑当前用户的 crontab 任务列表。
-l：查看当前用户有哪些 crontab 任务。
-r：删除当前用户所有的 crontab 任务。注意：此命令会清空所有任务，若要删除部分任务，请进入编辑模式（-e）将要删除的任务前加上 # 注释掉即可。
-i：删除前会弹出确认提示，防止误删（建议与 -r 配合使用，即 crontab -ri）。
任务格式与时间字段
以一个例子来说明：
* * * * * /bin/ls -l /etc/ > /home/lzy/1.txt
意为每分钟执行一次 ls -l /etc/，并将其输出覆盖写入到 /home/lzy/1.txt 文件中。
其中 * * * * * 代表五个时间字段：
第一个 *：一小时中的第几分钟（范围：0-59）
第二个 *：一天中的第几个小时（范围：0-23）
第三个 *：一个月中的第几天（范围：1-31）
第四个 *：一年中的第几个月（范围：1-12）
第五个 *：一周中的星期几（范围：0-7，0 与 7 都表示周日）
  
特殊符号说明
*：表示任意时间（通配符）。例如第一个 * 表示每分钟都执行。
,：表示同一字段内的多个离散时间点。例如 0 8,12,16 * * * 表示在每天的 8:00、12:00、16:00 各执行一次。
-：表示连续的时间范围。例如 0 5 * * 1-6 表示在周一到周六的每天凌晨 5 点执行一次。
*/：表示每隔多久执行一次。例如 */10 * * * * 表示每 10 分钟执行一次。
举例
每隔一分钟将当前日期信息追加到 1.txt：
*/1 * * * * /bin/date >> /home/lzy/1.txt
完整的任务调度流程
首先使用 vim 编辑 shell 脚本，注意脚本首行必须加上解释器声明（如 #!/bin/bash）。
使用 chmod +x 脚本名.sh 赋予脚本可执行权限。
使用 crontab -e 进入任务调度编辑界面。
键入 crontab 命令，注意命令和文件务必使用绝对路径。例如：* * * * * /home/lzy/date.sh
补充：备份与恢复
在使用 crontab -r 删除前，建议先进行备份以防后悔：
备份：crontab -l > crontab_backup.txt
恢复：crontab < crontab_backup.txt（或 crontab crontab_backup.txt）
  
at 一次性定时任务
at 用于执行一次性的定时任务，执行完毕后不再重复。在执行前，atd 守护进程会在后台检查作业队列，默认情况下每 60 秒检查一次。如果有作业且运行时间与当前时间匹配，则运行此作业。
注意：在使用 at 命令前，一定要检查 atd 进程是否已启动：
ps -ef | grep atd
  
at 命令格式
at [-m] 时间
写完 at 命令并按回车后，会进入 at> 提示符，输入需要执行的命令，最后按 Ctrl + D 结束编辑并保存任务。（如果输入错误，可以使用 Ctrl + 退格键删除）
at 的时间定义方式
以小时分钟指定：如 04:00
使用模糊时间词：如 midnight（午夜）、noon（中午）、teatime（下午4点）
12小时制：如 12pm
具体的年月日时间：如 04:00 2021-03-01
相对计时：now + 数字 + 时间单位，如 now + 5 minutes（表示5分钟后执行）
日期关键字：直接使用 today、tomorrow 来指定
举例：在2天后的下午5点执行查看文件命令
输入 at 5pm + 2 days
在 at> 提示符下输入 ls /home/lzy/1.txt
按一次 Ctrl + D 退出并提交任务。
  
如何管理 at 任务
atq：查看当前有哪些 at 任务，会显示任务的编号。
atrm 编号：删除指定的 at 任务。
atrm（不带编号）：删除所有的 at 任务。



                                         
